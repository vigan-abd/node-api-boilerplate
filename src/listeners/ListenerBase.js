class ListenerBase {
  /**
   * Runs each time an event is emitted
   * @param {any} event EventBase type or other data type that will be passed to listener
   * @memberof ListenerBase
   */
  handle(event) {

  }
}

module.exports = ListenerBase;